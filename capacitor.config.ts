import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cubancigarfactory.leads',
  appName: 'Cuban Cigar App',
  webDir: 'dist'
};

export default config;
